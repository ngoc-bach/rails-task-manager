<h1>My tasks</h1>

<ul class="list-group">
  <% @tasks.each do |task| %>
    <li class="list-group-item">
    <input class="form-check-input me-1" type="checkbox"><%= link_to task.title, task_path(task) %>
    </li>
  <% end %>
</ul>

<%= link_to "Add new task", new_task_path %>
